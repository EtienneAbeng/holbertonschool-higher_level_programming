-- <SELECT> Sélectionne et calcule la moyenne des scores de tous les enregistrements dans la table "second_table"
SELECT AVG(score) AS average

-- <FROM> Extrait les données de la table "second_table" de la base de données "hbtn_0c_0"
FROM second_table;
